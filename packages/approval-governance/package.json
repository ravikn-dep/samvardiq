{
  "name": "@samvardiq/approval-governance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Approval & Governance Layer (SOSA Layer 6): Recommendation -> Approval Request -> Human Decision -> Approval Record. No automation/execution.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build --prefix ../marketing-intelligence",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "lint": "eslint src test"
  },
  "devDependencies": {
    "@samvardiq/marketing-intelligence": "file:../marketing-intelligence",
    "@types/node": "^22.10.0",
    "eslint": "^9.15.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.16.0"
  }
}
